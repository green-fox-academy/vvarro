package com.greenfoxacademy.frontend.ArrayHandler;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class ArrayHandlerController {

//  @RequestMapping(value = "/arrays", method = RequestMethod.POST)
  //public ArrayHandler arrayHandler(@RequestBody Input[] input) {
    //ArrayHandler arrayHandler = new ArrayHandler();
   // if (what.equals("sum")) {
   //   doUntil.sum(input.getUntil());
   // } else if (what.equals("factor")) {
   //   doUntil.factor(input.getUntil());
   // }
   // return arrayHandler;
//  }


}
